version: "3.9"

services:
  portfolio-app:
    container_name: app
    image: app
    restart: always
    build:
      context: ./app
      dockerfile: Dockerfile
      target: base
    ports:
      - "${PORT}:${PORT}"
